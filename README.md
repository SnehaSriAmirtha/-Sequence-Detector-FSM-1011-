# -Sequence-Detector-FSM-1011-
Design a finite state machine that detects the sequence '1011' in a serial input stream. Implement both overlapping and non-overlapping versions. Create Moore and Mealy machine implementations. 
